<div class="row">
  <div class="col-md-6 col-md-offset-3">

    <div class="page-header">
      <h1>Account: <em><%= @account.name %></em></h1>
      <p class="lead">Edit account info</p>
    </div>

    <div class="row">
      <div class="col-md-12">

        <%= form_for @account, html: { class: "form-group", role: "form" } do |f| %>
          <div class="form-group">
            <%= f.label :name, class: "control-label" %>
            <%= f.text_field :name, class: 'form-control' %>
          </div>
          <div class="form-group">
            <%= f.submit 'Update Account', class: 'btn btn-default' %>
          </div>
        <% end %>

        <hr>

        <h4><%= link_to "Cancel", @account %></h4>

      </div>
    </div>

  </div>
</div>

<!-- <h1>Accounts#edit</h1>
<p>Find me in app/views/accounts/edit.html.erb</p>

<%= form_for @account, html: { class: "form-inline", role: "form" } do |f| %>
  <div class="form-group">
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.submit 'Update Account', class: 'btn btn-default' %>
  </div>
<% end %>

<p>
  <%= link_to "Cancel", @account %>
</p> -->
