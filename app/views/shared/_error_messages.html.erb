<% if object.errors.any? %>
  <div class="alert alert-danger">
    Uh oh! we found <%= pluralize(object.errors.count, "error") %>
  </div>
  <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  </ul>
<% end %>
